# Default

This example illustrates the default setup, in its simplest form.

## Types

```hcl
instance = object({
  resource_group = string
  location       = string
  name           = string
  sku_name       = string
  sku_tier       = string
})
```
